# This values file just tests secrets store csi with the AWS provider
# Only using single database credentials secret

database:
  external:
    enabled: true
    credentials:
      type: sscsi
      provider: aws
    databases:
    - secretArn: "arn:aws:secretsmanager:us-east-1:1234567890:secret:secretname"
      secretName: k8sSecretName
      module: clustermgr
      urlKey: modified-url-key
      portKey: modified-port-key
      userKey: modified-username-key
      passKey: modified-password-key
      dbnameKey: modified-dbname-key
    - secretArn: "arn:aws:secretsmanager:us-east-1:1234567890:secret:secretname2"
      secretName: k8sSecretName2
      module: persistence
    - secretArn: "arn:aws:secretsmanager:us-east-1:1234567890:secret:secretname3"
      secretName: k8sSecretName3
      module: audit
      # Default key names

# This defines the minimum required settings to use this Helm Chart.
image:
  credentials:
    type: values
