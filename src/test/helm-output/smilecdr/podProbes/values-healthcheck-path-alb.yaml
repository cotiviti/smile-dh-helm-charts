# This values file tests if the endpoint health path gets picked up in the readiness probe

ingress:
  enabled: true
  type: aws-lbc-alb

modules:
  fhir_endpoint:
    config:
      endpoint_health.path: /differenthealthcheckpath
