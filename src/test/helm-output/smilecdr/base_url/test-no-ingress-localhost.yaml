
# This file defines configurations specific for this test

specs:
  hostname: smile.com

ingresses:
  default:
    enabled: true

modules:
  fhir_endpoint:
    service:
      ingresses:
        default:
          enabled: false
    config:
      base_url.fixed: localhost
