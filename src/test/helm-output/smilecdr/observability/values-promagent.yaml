observability:
  prometheus:
    # Sets up pod monitor CR so the prometheus operator can discover Smile CDR
    # operatorConfig:
    #   enabled: false
    # Install Java Agent into Smile CDR pods to expose JVM metrics
    jvmMetrics:
      enabled: true
    # Allow scraping of metrics from admin_json endpoint
    # smilecdrMetrics:
    #   enabled: false
