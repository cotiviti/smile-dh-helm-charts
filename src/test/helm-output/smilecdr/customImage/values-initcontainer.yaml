# This file ensures the custom image is also used for the sync init containers

image:
  repository: custom.image/repo/image
  tag: "custom-tag"

copyFiles:
  # Copies files from S3 to the classes directory
  classes:
    #syncDefaults: false
    sources:
    - type: curl
      fileName: file.jar
      url: https://source.example.com/file.jar
  # customerlib:
  #   #syncDefaults: false
  #   sources:
  #   - type: curl
  #     fileName: file.jar
  #     url: https://source.example.com/file.jar
