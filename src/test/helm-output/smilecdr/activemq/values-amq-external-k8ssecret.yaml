# This values files enables provisioning Kafka using the Strimzi operator
messageBroker:
  external:
    enabled: true
    type: activemq
    config:
      connection:
        address: tcp://localhost:61616
      authentication:
        type: k8sSecret
        secretName: amqsecret
