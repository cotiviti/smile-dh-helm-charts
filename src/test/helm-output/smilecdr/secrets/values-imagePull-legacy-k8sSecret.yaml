# This file defines the minimum required settings to use this Helm Chart.

image:
  # Check that the old deprecated method still works
  repository: docker.smilecdr.com/smilecdr
  credentials:
    type: k8sSecret
    pullSecrets:
    - name: myK8sSecretName
