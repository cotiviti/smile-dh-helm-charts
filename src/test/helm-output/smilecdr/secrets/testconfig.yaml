tests:
  sscsiDbOnlyAWS:
    outputFile: output-sscsiDbOnly-aws.yaml
    valueFiles:
    - values-sscsiDbOnly-aws.yaml

  sscsiImagePullSecretAndDbAWS:
    outputFile: output-sscsiImagepullsecret-and-db-aws.yaml
    valueFiles:
    - values-sscsiImagepullsecret-and-db-aws.yaml

  sscsiImagePullSecretOnlyAWS:
    outputFile: output-sscsiImagepullsecret-only-aws.yaml
    valueFiles:
    - values-sscsiImagepullsecret-only-aws.yaml

  sscsiMultipleSecretMultipleDbAWS:
    outputFile: output-sscsiMultipleSecretMultipleDb-aws.yaml
    valueFiles:
    - values-sscsiMultipleSecretMultipleDb-aws.yaml

  imagePullK8sSingle:
    outputFile: output-imagePull-k8s-single.yaml
    valueFiles:
    - values-imagePull-k8s-single.yaml

  imagePullK8sMultiple:
    outputFile: output-imagePull-k8s-multiple.yaml
    valueFiles:
    - values-imagePull-k8s-multiple.yaml

  imagePullK8sSscsiMultiple:
    outputFile: output-imagePull-k8s-sscsi-multiple.yaml
    valueFiles:
    - values-imagePull-k8s-sscsi-multiple.yaml

  # Test the deprecated method
  imagePullLegacySSCSI:
    outputFile: output-imagePull-legacy-sscsi.yaml
    valueFiles:
    - values-imagePull-legacy-sscsi.yaml

  imagePullLegacyK8sSecret:
    outputFile: output-imagePull-legacy-k8sSecret.yaml
    valueFiles:
    - values-imagePull-legacy-k8sSecret.yaml

  imagePullLegacyValues:
    outputFile: output-imagePull-legacy-values.yaml
    valueFiles:
    - values-imagePull-legacy-values.yaml
